[package]
name = "storage"
version = "0.1.0"
edition = "2021"
documentation = "Embedded storage replacement"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[features]
default = ["std"]
std = []
